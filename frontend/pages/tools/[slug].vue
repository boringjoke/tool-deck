<script setup lang="ts">
import { defineAsyncComponent, onMounted } from 'vue'
import { useToolPreferences } from '~/composables/useToolPreferences'
import { findPublicToolBySlug } from '~/registry'
import { getToolComponentLoader } from '~/tools'

const route = useRoute()
const tool = findPublicToolBySlug(String(route.params.slug))

if (!tool) {
  throw createError({
    statusCode: 404,
    message: '工具不存在或尚未公开',
  })
}

const { isFavorite, recordRecent, toggleFavorite } = useToolPreferences()
const toolComponentLoader = getToolComponentLoader(tool.slug)

if (!toolComponentLoader) {
  throw createError({
    statusCode: 500,
    message: '工具模块尚未接入',
  })
}

const ToolComponent = defineAsyncComponent(toolComponentLoader)

const timerInfo = {
  usage: [
    '选择正计时或倒计时模式；倒计时输入小时、分钟和秒。',
    '点击“开始”启动计时，可以暂停、继续或重置当前计时任务。',
    '倒计时归零后会显示完成状态和到时提醒；需要新的倒计时可以重置后重新开始。',
  ],
  notes: [
    '首版只支持一个当前计时任务；分钟和秒允许输入 60，99:60:60 会显示为 100:01:00.000，空白字段按 0 处理。',
    '页面恢复可见、重新获得焦点或恢复页面生命周期时会按时间戳重新同步；浏览器挂起期间不保证即时提醒。',
    '到时提醒只使用页面文字和可访问性播报，不申请系统通知、不自动播放音频、不使用振动。',
    '所有输入、状态和结果仅保留在当前页面内存，不会写入 URL、浏览器存储或上传服务器。',
  ],
}

const coinFlipInfo = {
  usage: [
    '点击“抛一次”抛掷一枚硬币，查看正面或反面结果。',
    '可以连续抛掷；每次成功抛掷都会替换当前结果并更新连抛统计。',
    '需要重新开始统计时点击“清空”，也可以复制当前结果和统计摘要。',
  ],
  notes: [
    '统计只保留当前页面会话的总次数、正面次数和反面次数，不保存历史，也不做概率预测。',
    '使用浏览器 Web Crypto 安全随机源，不使用 Math.random()；安全随机源不可用时不会生成结果。',
    '默认使用简洁翻转动画；开启减少动态效果后会直接显示结果，动画不影响随机结果。',
    '结果和统计只保留在当前浏览器页面内存，不会写入 URL、浏览器存储或上传服务器。',
  ],
}

const diceInfo = {
  usage: [
    '选择 1–6 枚标准六面骰子，点击“掷骰子”生成每枚骰子的点数和本次总和。',
    '可以重复投掷；每次成功投掷会替换当前结果，并保留最近 10 次当前页面会话记录。',
    '需要重新开始时点击“清空”，也可以复制当前一次投掷的结果和点数总和。',
  ],
  notes: [
    '当前版本只支持标准六面骰子；未来常用 DND 骰子扩展需要另行确认面数和交互范围。',
    '使用浏览器 Web Crypto 安全随机源和无偏取样，不使用 Math.random()；安全随机源不可用时不会生成结果。',
    '默认使用约 420ms 的骰子滚动动画；开启减少动态效果后会直接显示结果，动画不影响随机结果。',
    '当前结果和最近 10 次记录只保留在当前页面内存，不会写入 URL、浏览器存储或上传服务器。',
  ],
}

const wheelInfo = {
  usage: [
    '编辑转盘选项和前面各项的占比；最后一项占比会自动计算到总和 100.0%。',
    '新增选项后可以点击“平均分配”，让当前选项按 0.1% 精度尽可能均分。',
    '点击“开始转盘”按当前占比抽取一个选项；需要时可以复制本次结果或清空恢复初始配置。',
    '开启“去除选中”后，成功选中的选项会在动画完成后移除，其占比平均分给剩余选项。',
  ],
  notes: [
    '首版只支持一个当前转盘，最多 20 个选项；占比使用 0.1% 精度，0.0% 的选项保留但不会被抽中。',
    '去除选中后的平均分配按当前选项顺序处理无法整除的 0.1% 余数；最后一个选项被移除后当前转盘会耗尽。',
    '使用浏览器 Web Crypto 安全随机源，不使用 Math.random()；转盘动画约 1500ms，动画不影响随机结果。',
    '选项、占比、结果和去除状态只保留在当前页面内存，不会写入 URL、浏览器存储或上传服务器。',
  ],
}

const marqueeInfo = {
  usage: [
    '输入需要展示的文字，调整字号、文字颜色、背景颜色和滚动速度。',
    '点击“开始展示”后，文字会从右向左滚动；浏览器支持时会进入全屏并尝试横屏和保持屏幕常亮。',
    '展示结束后点击“结束展示”返回配置状态；全屏、横屏或屏幕常亮不可用时仍可继续使用页面内展示。',
  ],
  notes: [
    '首版只支持一条当前展示内容；文字最多 200 个 Unicode 字符，换行会转换为空格，字号为 24–160px，速度为五档。',
    '开启系统减少动态效果后会使用静态居中大字展示；浏览器能力失败只显示提示，不会阻断展示。',
    '所有输入和展示状态只保留在当前浏览器页面内，不会写入 URL、浏览器存储或上传服务器。',
  ],
}

const metronomeInfo = {
  usage: [
    '设置每分钟节拍数 BPM、每小节拍数和页面音量。',
    '点击“开始”后，浏览器会在用户手势中解锁本地音频并播放合成节拍；每小节第一拍使用重拍。',
    '运行期间配置会锁定；点击“停止”释放音频资源，之后可以修改配置并重新开始。',
  ],
  notes: [
    'BPM 支持 40–240 的整数，默认 120；每小节支持 1–12 拍，默认 4；页面音量支持 0–100%，默认 70%。',
    '首版不支持 tap tempo、节奏细分、复杂节奏编排、训练统计、MIDI、录音和文件导入。',
    '声音由浏览器本地 Web Audio 合成，不使用远程音频文件；浏览器拒绝音频解锁时不会进入运行状态。',
    '浏览器切换后台或系统挂起音频时不补播积压节拍；恢复后可点击“恢复音频”从未来节拍继续。',
    '输入、配置、运行状态和节拍记录只保留在当前页面内，不会上传、写入 URL 或浏览器存储。',
  ],
}

const prompterInfo = {
  usage: [
    '粘贴演讲稿、台词或拍摄提纲，调整字号和五档滚动速度。',
    '需要镜面反射时开启“水平镜像”，点击“开始滚动”后会从稿件顶部沿阅读线向上移动。',
    '播放中可以暂停、继续或停止；滚动到稿件末尾会自动结束，下一次开始仍从顶部重新播放。',
  ],
  notes: [
    '首版支持单条稿件，最多 20,000 个 Unicode 字符；保留内部换行，字号为 32–96px，默认 56px。',
    '滚动速度为 24、40、60、90、130 px/s 五档；运行或暂停期间会锁定稿件、字号、速度和镜像配置。',
    '浏览器支持时会尝试全屏、横屏锁定和屏幕常亮；任一能力失败都只显示提示，不阻断内嵌舞台播放。',
    '页面隐藏会冻结当前位置并暂停，返回后不会自动继续；不支持循环、拖动定位、段落跳转、文件导入、摄像头或麦克风。',
    '稿件、配置和播放状态只保留在当前页面内存，不会上传、写入 URL 或浏览器存储。',
  ],
}

const characterCountInfo = {
  usage: [
    '在输入框中输入或粘贴需要统计的文本。',
    '实时查看字符、词数、句子、段落和行数等结果。',
    '需要时复制统计结果，或清空输入重新开始。',
  ],
  notes: [
    '字符按用户可见的 Unicode 字素统计；不含空格会排除 Unicode 空白字符。',
    '词数和句子使用浏览器标准分词能力，段落按空行分隔，行数按换行符统计。',
    '所有处理在当前浏览器本地完成，输入不会上传至服务器。',
  ],
}

const morseCodeInfo = {
  usage: [
    '选择“文本转摩斯码”或“摩斯码转文本”方向。',
    '输入英文字母、数字、已支持标点或按规则分隔的摩斯码，点击“开始转换”。',
    '转换成功后可以复制结果；点击“清空”会恢复默认方向并清除当前状态。',
  ],
  notes: [
    '首版使用国际摩斯码，支持英文字母、数字和已列出的常用标点；中文、Emoji、全角标点和其他未列符号不会自动转码。',
    '文本转摩斯码时字符代码使用空格分隔、单词使用“/”分隔；摩斯码转文本时非法代码、空代码段和错误分隔会明确提示。',
    '原始输入最多 10,000 个 Unicode 字符；工具不会自动截断、纠错、模糊匹配或静默丢弃不支持内容。',
    '所有输入、结果和错误仅在当前浏览器页面内处理，不会请求网络、播放音频或写入 URL、浏览器存储和其他持久化位置。',
  ],
}

const urlToolInfo = {
  usage: [
    '选择解析与参数编辑、URL 编码或 URL 解码模式。',
    '解析模式输入包含协议的完整绝对 URL，查看 Origin、Path 和查询参数。',
    '编辑、增加或删除参数后，可重建完整 URL，或生成 SQL WHERE IN 条件。',
    '编码或解码模式选择 URL 组件或完整 URL 语义，再执行对应操作。',
  ],
  notes: [
    '解析模式要求完整绝对 URL；重建 URL 时会使用标准组件编码，原始编码形式可能被规范化。',
    '重复参数、空值和参数顺序会被保留；SQL 会按参数名分组并转义标识符和字符串。',
    '解析不会发起网络请求，也不会检测 URL 是否可访问。',
    '所有输入和结果仅在当前浏览器页面内处理，不会上传或写入持久化存储。',
  ],
}

const timestampInfo = {
  usage: [
    '选择时间戳转日期时间，或日期时间转时间戳。',
    '时间戳模式选择秒/毫秒后输入整数；日期模式默认使用日期时间选择器。',
    '需要时切换到 ISO 8601 文本，选择本地时间或 UTC，再点击“开始转换”。',
    '也可以使用“使用当前时间”快捷填入当前值，并复制转换结果。',
  ],
  notes: [
    '日期时间选择器输入不带时区，按上方的本地时间/UTC选项解释。',
    'ISO 8601 文本支持无时区、Z、显式偏移和毫秒；带时区的输入按自身时区语义解析。',
    '本地时间使用当前浏览器时区；夏令时边界可能存在本地时间歧义。',
    '所有转换在浏览器本地完成，不读取网络时间，也不会保存或上传输入。',
  ],
}

const dateTimeCalculatorInfo = {
  usage: [
    '选择“日期差”或“日期加减”模式。',
    '日期差模式输入开始和结束日期，时间可选；日期加减模式输入基准日期、可选时间、增加或减少的数值和单位。',
    '点击“开始计算”查看结果，需要时复制结果或清空当前状态。',
  ],
  notes: [
    '日期差按照“结束时间 - 开始时间”计算，日期加减支持年、月、日、小时、分钟和秒。',
    '时间留空时按当天 00:00:00.000 计算；填写时间可以精确到毫秒。',
    '年和月按日历规则处理；目标月份没有原日期日号时，会使用该月最后一天。',
    '计算使用当前浏览器本地时区，夏令时边界可能影响实际时间差；不提供跨时区数据库级计算。',
    '所有输入和结果仅在当前浏览器页面内处理，不会上传或写入持久化存储。',
  ],
}

const worldTimeInfo = {
  usage: [
    '从联合国成员国及观察员国的首都目录中搜索并添加城市，也可以保留北京、东京、新加坡、悉尼、迪拜、伦敦、纽约和洛杉矶等默认城市。',
    '使用中文名、英文名、国家名或 IANA 时区搜索目录，并从下拉选项中添加城市。',
    '移除不需要的城市；时间会在浏览器本地每秒更新。',
    '点击“清空城市”可以清除当前列表和实时结果。',
  ],
  notes: [
    '当前只显示当前实时世界时间，已选城市或时区最多 12 个；不提供固定日期转换、网络校时、闹钟、倒计时或日历能力。',
    '首都目录包含中文名、英文名、国家名和 IANA 时区，按英文名称首字母排序；当前版本不处理首都变更历史。',
    '自定义输入必须是浏览器支持的 IANA 时区，例如 Asia/Shanghai、Europe/London 或 America/New_York；不接受城市名称或 UTC 偏移文本。',
    '夏令时和 UTC 偏移由当前浏览器的 Intl 时区数据处理；系统时间不准确时，工具不会向网络校准。',
    '城市选择、自定义时区和时间结果只保留在当前页面内存，不会请求网络、写入浏览器存储或上传服务器。',
  ],
}

const uuidInfo = {
  usage: [
    '设置生成数量，并选择是否使用大写字母。',
    '点击“生成 UUID”创建新的标准 UUID v4 结果。',
    '需要时复制全部结果，或导出为每行一个 UUID 的 TXT 文件。',
  ],
  notes: [
    '生成数量和 UUID v4 输出格式遵循批次 1A 已确认规则。',
    '每次生成都会重新使用浏览器安全随机源，不使用 Math.random()。',
    '结果仅保留在当前页面内存中，不会写入 URL、收藏、最近使用或其他持久化存储。',
    '所有生成过程在浏览器本地完成，不会上传 UUID 或请求外部服务。',
  ],
}

const passwordInfo = {
  usage: [
    '设置账号总长度、前缀和后缀，再设置密码长度、生成数量和字符类型。',
    '点击“生成账号密码”创建成对结果；需要时复制全部结果或清空当前页面状态。',
    '修改配置后可以重复生成，账号和密码的生成数量保持一致。',
  ],
  notes: [
    '密码长度为 4–32，生成数量为 1–100；账号总长度为 4–32，前缀和后缀计入总长度。',
    '启用多种密码字符类型时，每个已选类型至少出现一次；前缀和后缀只允许字母、数字、点、下划线和短横线。',
    '生成使用浏览器 Web Crypto 安全随机源，不使用 Math.random()；没有安全随机源时不会展示部分结果。',
    '账号和密码只保留在当前页面内存中，不提供账号管理、密码保存、密码历史或云端生成，也不会上传服务器。',
  ],
}

const qrCodeInfo = {
  usage: [
    '输入需要编码的文本或 URL，选择导出尺寸后点击“生成二维码”。',
    '生成成功后查看二维码预览，并按需导出 SVG 或 PNG 文件。',
    '修改输入或尺寸后需要再次点击“生成二维码”；点击“清空”会清除当前输入和结果。',
  ],
  notes: [
    'URL 只作为文本编码，不会检测可访问性；首尾空白只用于空值判断，实际编码会保留原始内容。',
    '输入上限为 2048 个 UTF-8 字节；二维码版本自动选择，固定使用 M 纠错级别、4 模块静区和黑白配色。',
    '导出尺寸支持 256、512 和 1024 像素；文件名只包含工具标识和本地日期时间，不包含输入内容。',
    '所有输入、预览和生成过程仅在当前浏览器页面内处理，不会写入 URL、浏览器存储或上传服务器。',
  ],
}

const barcodeInfo = {
  usage: [
    '选择 CODE128、CODE39、EAN-13 或 UPC-A 码制，输入内容后点击“生成”。',
    '生成成功后查看条形码预览，并按需导出 SVG 或 PNG 文件。',
    '修改输入或码制后需要再次点击“生成”；点击“清空”会清除结果并恢复 CODE128。',
  ],
  notes: [
    'CODE128 支持可见 ASCII 字符，最多 80 个；CODE39 支持大写字母、数字、空格和 - . $ / + %，最多 40 个。',
    'EAN-13 接受 12 位主体或校验正确的 13 位完整编码；UPC-A 接受 11 位主体或校验正确的 12 位完整编码，主体输入会自动补校验位。',
    '预览和导出固定使用黑白配色、2px 条宽、100px 条高和 10px 静区；文件名不包含输入内容。',
    '所有输入、预览和导出过程仅在当前浏览器页面内处理，不会请求网络或写入浏览器存储。',
  ],
}

const shieldImageInfo = {
  usage: [
    '输入左侧文字、右侧文字和两侧背景色，点击“生成 Shield”。',
    '生成成功后查看双区域 SVG 预览，可以复制 SVG，或导出 SVG 和 PNG 文件。',
    '修改文字或颜色后需要再次点击“生成 Shield”；点击“清空”会恢复默认颜色并清除当前结果。',
  ],
  notes: [
    '首版使用固定扁平样式；文字每侧最多 32 个 Unicode 字符，不允许换行和控制字符。',
    '背景色只支持 #RGB 或 #RRGGBB 格式；PNG 由浏览器本地 Canvas 生成，不提供 JPG、Logo、渐变、批量生成或在线 Shield 服务。',
    '生成、预览、复制和导出都在当前浏览器本地完成，文件名不包含输入文字或颜色。',
    '所有输入、结果和错误仅保留在当前页面内存，不会请求网络、写入浏览器存储或上传服务器。',
  ],
}

const signatureInfo = {
  usage: [
    '在画布上使用鼠标、触摸或触控笔写下签名；一次按下到抬起算作一笔。',
    '需要修正时点击“撤销一笔”，也可以直接清空后重新绘制。',
    '签名准备好后点击“导出透明 PNG”，文件会保存到浏览器默认下载位置。',
  ],
  notes: [
    '画布逻辑尺寸固定为 1200 × 400 px，页面缩放不会改变导出尺寸；笔迹使用固定深色和固定线宽。',
    '首版只导出透明 PNG，不支持导入签名、文字签名、橡皮擦、压感、SVG 或签名历史。',
    '空画布不会生成文件；Canvas 或浏览器下载能力失败时会保留当前笔画并显示错误。',
    '签名和导出过程只保留在当前页面内存，不会写入 URL、浏览器存储、日志或上传服务器。',
  ],
}

const base64ImageInfo = {
  usage: [
    '选择“图片 → Base64”并上传一张 PNG、JPEG、WebP 或 GIF 图片，点击“转换为 Base64”。',
    '在结果中分别复制或下载 Data URL、纯 Base64 文本；切换到“Base64 → 图片”可粘贴 Data URL 或纯 Base64 生成预览。',
    '粘贴纯 Base64 时选择对应图片格式；Data URL 会自动读取 MIME 类型，生成成功后可以下载原始图片。',
  ],
  notes: [
    '单张原始或解码后图片最大 10 MiB；首版仅支持 PNG、JPEG、WebP 和 GIF，不支持 SVG、批量处理、远程 URL、压缩、裁剪或格式转换。',
    'Base64 会忽略普通空白并校验标准字母表、长度和填充；会同时检查 MIME 与图片文件签名，不匹配或无法识别时不会预览或下载。',
    '预览由原始图片字节生成，不通过 Canvas 重编码；文本文件名为 base64-image-data-url-YYYYMMDD.txt 或 base64-image-raw-YYYYMMDD.txt，图片文件名为 base64-image-YYYYMMDD.扩展名。',
    '输入、预览、对象 URL 和结果只保留在当前页面内存；切换模式、清空、刷新或离开页面后不恢复，也不会上传或写入浏览器存储。',
  ],
}

const imageWatermarkInfo = {
  usage: [
    '选择一张 PNG、JPEG 或 WebP 图片，输入水印文字并选择位置、字号、颜色和透明度。',
    '点击“生成水印”查看原图与结果对照；点击任一预览可放大查看细节，确认结果后下载跟随输入格式的图片文件。',
    '参数修改后需要重新生成；失败时会保留并标记上一次成功生成的结果。',
  ],
  notes: [
    '首版只支持单条文本水印，不支持 GIF、Logo/图片水印、多水印、平铺、批量处理、裁剪、缩放或格式转换。',
    '单张文件最大 10 MiB，解码后最多 20 MP，最长边最多 8,192 px；透明度范围为 10%–100%。',
    '输出保持原像素尺寸；PNG 无损导出，JPEG 使用固定质量 0.92，WebP 使用固定质量 0.90；Canvas 导出不保留 EXIF、ICC 等原文件元数据。',
    '文件名为 watermarked-image-YYYYMMDD.扩展名；输入、预览、对象 URL 和结果只保留在当前页面内存，不会上传或写入浏览器存储。',
  ],
}

const asciiArtInfo = {
  usage: [
    '选择一张 PNG、JPEG 或 WebP 图片，设置输出列数后点击“生成 ASCII 画”。',
    '生成成功后查看等宽字符预览，可以复制完整文本或下载 UTF-8 TXT 文件。',
    '更换图片或列数后需要重新生成；处理失败时会保留并标记上一次成功生成的结果。',
  ],
  notes: [
    '首版使用固定 10 档 ASCII 字符集和相对亮度映射，透明像素按白色背景处理；不支持 GIF、彩色输出、图片导出或批量处理。',
    '单张文件最大 10 MiB，解码后最多 20 MP，最长边最多 8,192 px；输出列数为 40、80、120、160 四档，默认 80 列。',
    '结果按固定字符网格生成，最多 240 行或 32,000 个字符单元；首版不启用 Worker，遇到超大输出会提示降低列数。',
    '图片读取、字符转换、复制和下载都在当前浏览器本地完成；输入、结果和错误不会上传或写入浏览器存储。',
  ],
}

const torrentMagnetInfo = {
  usage: [
    '选择一个本地 .torrent 文件，点击“分析并生成 Magnet”；选择文件后不会自动读取。',
    '分析成功后查看 Torrent 名称、文件摘要、Piece 信息、Tracker、v1 info-hash 和只读 Magnet 链接。',
    '按需复制 Magnet 或下载 UTF-8 TXT；更换文件后需要重新分析，点击“清空”会移除当前结果。',
  ],
  notes: [
    '首版仅支持经典 v1 Torrent；v2 或混合 Torrent 会明确提示暂不支持，不生成 Magnet。',
    '文件最大 10 MiB，严格校验 Bencode 字典排序、重复键、整数和尾随字节；多文件最多展示前 2,000 项，统计仍按完整元数据计算。',
    'Magnet 只生成 xt、可选 dn 和按原顺序去重的 tr；v1 info-hash 对原始 info 字典字节计算，不请求 Tracker、DHT 或其他网络资源。',
    '文件只在当前浏览器内存中处理，不上传、不写入 URL 或浏览器存储；Web Crypto 不可用时不会生成不完整链接。',
  ],
}

const coordinateConverterInfo = {
  usage: [
    '选择源坐标系和目标坐标系，输入一个点的纬度与经度十进制度数。',
    '点击“开始转换”查看结果；需要时可以交换转换方向，或复制带标签的纬度/经度文本。',
    '输入或转换方向改变后，点击“开始转换”更新结果；点击“清空”会恢复默认方向并清除当前状态。',
  ],
  notes: [
    '首版支持 WGS84、GCJ-02 和 BD-09 的六个两两转换方向；不支持 CGCS2000、Web Mercator、UTM、批量文件或地图服务。',
    '涉及 GCJ-02 或 BD-09 时，参考公式适用范围为纬度 3.86–53.55、经度 73.66–135.05；超出范围不会原值返回或生成结果。',
    '结果默认显示 6 位小数，属于近似转换，不适用于测绘、导航、航空、施工、边界确认、救援或其他高精度、安全关键场景。',
    '坐标仅在当前浏览器内存中处理，不请求地图或定位服务，不写入 URL、浏览器存储或上传服务器。',
  ],
}

const colorInfo = {

  usage: [
    '选择颜色输入格式，输入 HEX、RGB/RGBA 或 HSL/HSLA 颜色值后点击“转换颜色”。',
    '查看规范化的三类颜色结果；需要时使用原生取色器填入不透明颜色，或复制任一格式结果。',
    '在基础对比度区域输入前景色和背景色，点击“检查对比度”查看 WCAG AA/AAA 提示。',
    '点击“清空”会清除两组输入、结果和错误，恢复初始空状态。',
  ],
  notes: [
    'HEX 支持 #RGB、#RGBA、#RRGGBB 和 #RRGGBBAA；RGB 通道为 0–255 整数，HSL 色相为 0–小于 360，透明度为 0–1。',
    '数值越界不会自动截断或修正；不支持颜色名称、transparent、RGB 百分比、CSS Color 4 空格斜线语法、CMYK、HSV、渐变或变量。',
    '基础对比度只接受不透明颜色；图片/屏幕取色、EyeDropper API、调色板生成、文件处理和历史记录不在首版范围。',
    '所有输入、结果和转换过程仅在当前浏览器页面内处理，不会请求网络、写入浏览器存储或上传服务器。',
  ],
}

const baseInfo = {
  usage: [
    '设置源进制和目标进制，支持 2–36 的自定义进制。',
    '输入整数文本后点击“开始转换”，查看目标进制结果。',
    '需要时开启输出大写字母或 2/8/16 进制前缀，再复制结果或清空当前状态。',
  ],
  notes: [
    '支持正负号、与源进制匹配的 0b、0o、0x 前缀和最多 1024 个字符的整数文本。',
    '转换使用 BigInt 处理大整数；不支持小数、科学计数法或近似转换，非法字符会提示位置和源进制。',
    '输出前缀仅适用于二进制、八进制和十六进制；大小写选项只影响进制字母和对应前缀。',
    '所有输入和结果仅在当前浏览器页面内处理，不会上传或写入持久化存储。',
  ],
}

const unitConverterInfo = {
  usage: [
    '选择长度、面积、体积、质量、温度或速度类别。',
    '输入十进制数值，选择源单位和目标单位，点击“开始换算”。',
    '换算成功后可以复制结果；点击“清空”会恢复默认类别和单位。',
  ],
  notes: [
    '首版支持固定单位集合；美制液量盎司和美制加仑会明确标注为美制。',
    '不接受逗号、指数、单位后缀或表达式；温度允许负数，其他类别只接受零或正数。',
    '结果最多保留 12 位有效数字并去除无意义尾零，浮点换算可能存在表示精度边界。',
    '所有输入和结果仅在当前浏览器页面内处理，不会上传或写入持久化存储。',
  ],
}

const simplifiedTraditionalInfo = {
  usage: [
    '选择“简体转繁体”或“繁体转简体”方向。',
    '输入或粘贴需要转换的中文文本，点击“开始转换”。',
    '转换成功后可以复制结果；点击“清空”会清除当前状态并恢复默认方向。',
  ],
  notes: [
    '使用 OpenCC 标准词库完成确定性、词组级转换；首版不包含台湾或香港地域词、自定义词库和在线翻译。',
    '非中文字符、数字、标点、空格、换行和未命中的字符会保留；工具不会自动识别输入语言。',
    '转换失败时会保留输入和上一次成功结果；方向切换会清空输入，不会自动转换。',
    '所有输入和结果仅在当前浏览器页面内处理，不会上传或写入持久化存储。',
  ],
}

const numberToChineseInfo = {
  usage: [
    '选择数字转中文大写数字、数字转中文大写金额或中文大写转阿拉伯数字。',
    '输入对应格式的文本，点击“开始转换”；切换方式不会自动执行转换。',
    '转换成功后可以复制结果；点击“清空”会恢复默认模式并清除当前状态。',
  ],
  notes: [
    '中文大写数字使用零、壹、贰等规范字符；不接受普通中文数字、混合格式、科学计数法或货币符号。',
    '金额固定使用元、角、分，最多支持两位小数，不会自动截断或四舍五入；整数范围支持到载级（小于 10^48）。',
    '转换失败时会保留输入和上一次成功结果；结果只在当前浏览器页面内处理，不会上传或写入持久化存储。',
  ],
}

const jsonWorkbenchInfo = {
  usage: [
    '输入或粘贴标准 JSON，在“格式化 / 压缩 / 扁平化 / 反扁平化”之间选择处理方式。',
    '格式化、扁平化和反扁平化可选择 2 个空格、4 个空格或 Tab 缩进；压缩模式不使用缩进。',
    '点击对应的“开始”按钮后生成结果；切换模式不会自动执行处理。',
    '结果成功后可以复制；点击“清空”会清除输入、结果和错误状态。',
  ],
  notes: [
    '只接受标准 JSON 语法，不支持注释、单引号、尾逗号、undefined、NaN 或 Infinity。',
    '对象重复键遵循浏览器标准解析行为，后出现的同名键会覆盖先出现的值；超出 JavaScript 安全整数精度的数字可能发生精度变化。',
    '扁平化使用点号路径，路径中的点号和反斜线会转义；支持嵌套数组和根数组，根字符串、数字、布尔值和 null 不支持扁平化或反扁平化。',
    '反扁平化要求数组索引从 0 开始连续；路径冲突、非法路径和危险键会拒绝处理。格式化、扁平化和反扁平化生成可阅读 JSON，压缩只移除结构性空白。',
    '失败时会保留上一次成功结果。',
    '所有输入、结果和错误都只在当前浏览器页面内处理，不设置输入硬上限，不会上传 JSON、写入 URL 或持久化存储，也不提供文件下载。',
  ],
}

const jsonToSqlInfo = {
  usage: [
    '输入单个 MySQL 表名，并粘贴由扁平对象组成的非空 JSON 数组。',
    '点击“生成 SQL”将数组转换为一条 MySQL 多行 INSERT 语句。',
    '生成成功后可以复制 SQL，或下载为本地 .sql 文件；点击“清空”会重置当前状态。',
  ],
  notes: [
    '首版只支持 MySQL 和扁平 JSON 对象数组；所有记录必须包含相同字段，字段值只允许字符串、数字、布尔值和 null。',
    '嵌套对象、数组、UPDATE、WHERE、多方言、数据库连接和 SQL 执行不在首版范围；生成的 SQL 需要由你在目标环境中自行审查。',
    '表名和字段名会作为 MySQL 标识符安全转义，字符串中的单引号和反斜线会转义；工具不会执行输入内容。',
    '输入、结果、表名和错误只保留在当前浏览器页面内；复制和下载均需用户主动操作，不会上传或写入持久化存储。',
  ],
}

onMounted(() => {
  recordRecent(tool.slug)
})

usePublicSeo({
  title: `${tool.title} · Tool Deck`,
  description: tool.description,
  pathname: `/tools/${tool.slug}`,
})
</script>

<template>
  <ToolPageShell
    :tool="tool"
    :info="
      tool.slug === 'timer'
        ? timerInfo
        : tool.slug === 'coin-flip'
        ? coinFlipInfo
        : tool.slug === 'dice'
          ? diceInfo
        : tool.slug === 'wheel'
          ? wheelInfo
        : tool.slug === 'marquee'
          ? marqueeInfo
        : tool.slug === 'metronome'
          ? metronomeInfo
        : tool.slug === 'prompter'
          ? prompterInfo
        : tool.slug === 'character-count'
        ? characterCountInfo
        : tool.slug === 'morse-code'
          ? morseCodeInfo
        : tool.slug === 'url-tool'
          ? urlToolInfo
          : tool.slug === 'timestamp'
            ? timestampInfo
          : tool.slug === 'date-time-calculator'
              ? dateTimeCalculatorInfo
            : tool.slug === 'world-time'
              ? worldTimeInfo
            : tool.slug === 'uuid-generator'
              ? uuidInfo
                : tool.slug === 'password-generator'
                  ? passwordInfo
                : tool.slug === 'qr-code'
                  ? qrCodeInfo
                : tool.slug === 'barcode'
                  ? barcodeInfo
                : tool.slug === 'shield-image'
                  ? shieldImageInfo
                : tool.slug === 'signature'
                  ? signatureInfo
                : tool.slug === 'base64-image'
                  ? base64ImageInfo
                : tool.slug === 'image-watermark'
                  ? imageWatermarkInfo
                : tool.slug === 'ascii-art'
                  ? asciiArtInfo
                : tool.slug === 'torrent-magnet'
                  ? torrentMagnetInfo
                : tool.slug === 'coordinate-converter'
                  ? coordinateConverterInfo
                : tool.slug === 'color-tool'
                  ? colorInfo
                : tool.slug === 'simplified-traditional'
                  ? simplifiedTraditionalInfo
                : tool.slug === 'number-to-chinese'
                  ? numberToChineseInfo
                : tool.slug === 'unit-converter'
                  ? unitConverterInfo
                : tool.slug === 'json-workbench'
                  ? jsonWorkbenchInfo
                : tool.slug === 'json-to-sql'
                  ? jsonToSqlInfo
                : tool.slug === 'base-converter'
                  ? baseInfo
                  : undefined
    "
    :is-favorite="isFavorite(tool.slug)"
    @toggle-favorite="toggleFavorite(tool.slug)"
  >
    <component :is="ToolComponent" />
  </ToolPageShell>
</template>
