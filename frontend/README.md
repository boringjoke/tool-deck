# frontend

Tool Deck 的 Nuxt 4 前端工程。当前只完成阶段 0 的 Nuxt 迁移骨架，不包含具体工具实现。

## 本地命令

```sh
npm install
npm run dev
npm run type-check
npm run build
npm run generate
npm run preview
```

页面、工具注册、核心算法、浏览器适配器和测试目录按 `docs/阶段0工程设计.md` 逐步建立。
